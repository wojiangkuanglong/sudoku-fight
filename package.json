{
  "name": "sudoku-fight",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @sudoku-fight/shared build && concurrently -n server,web -c blue,green \"pnpm --filter @sudoku-fight/server dev\" \"pnpm --filter @sudoku-fight/web dev\"",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
